use_frameworks!

platform :ios, '9.0'

target 'LHypothesis_Example' do
  pod 'LHypothesis', :path => '../'

  target 'LHypothesis_Tests' do
    inherit! :search_paths

    pod 'Quick', '~> 2.2.0'
    pod 'Nimble', '~> 8.0.7'
  end
end
